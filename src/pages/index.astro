---
import { parameters, defaults } from '../config/screenshot';
---

<html lang="en">
<head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>WebShot - Screenshot Service</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
            line-height: 1.6;
        }
        h1 {
            color: #333;
            border-bottom: 2px solid #007acc;
            padding-bottom: 0.5rem;
        }
        h2 {
            color: #555;
            margin-top: 2rem;
        }
        pre {
            background: #f5f5f5;
            padding: 1rem;
            border-radius: 4px;
            overflow-x: auto;
            border: 1px solid #ddd;
        }
        code {
            font-family: 'Monaco', 'Menlo', 'Ubuntu Mono', monospace;
        }
        .endpoint {
            background: #e8f4fd;
            padding: 1rem;
            border-radius: 4px;
            border-left: 4px solid #007acc;
            margin: 1rem 0;
        }
        .example {
            background: #f0f8f0;
            padding: 1rem;
            border-radius: 4px;
            border-left: 4px solid #28a745;
            margin: 1rem 0;
        }
    </style>
</head>
<body>
    <h1>WebShot - Screenshot Service</h1>
    
    <div class="endpoint">
        <h3>API Endpoint</h3>
        <code>/api/take</code>
    </div>

    <div class="example">
        <h3>Example Usage</h3>
        <code>/api/take?url=https://example.com&viewport_width=1920&viewport_height=1080</code>
    </div>

    <h2>Available Parameters</h2>
    <pre><code>{JSON.stringify(parameters, null, 2)}</code></pre>

    <h2>Default Parameters</h2>
    <pre><code>{JSON.stringify(defaults, null, 2)}</code></pre>

    <h2>How to Use</h2>
    <p>Make a GET request to <code>/api/take</code> with your desired parameters as query strings. The service will return a screenshot image of the specified URL.</p>
    
    <p>For example, to take a screenshot of Google with custom dimensions:</p>
    <pre><code>/api/take?url=https://google.com&viewport_width=1366&viewport_height=768&format=png</code></pre>
</body>
</html>
