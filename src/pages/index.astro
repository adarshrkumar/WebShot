---
import { parameters, defaults } from '../config/screenshot';
---

<html lang="en">
<head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>ImageResize - Image Resizing API</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
            line-height: 1.6;
        }
        h1 {
            color: #333;
            border-bottom: 2px solid #007acc;
            padding-bottom: 0.5rem;
        }
        h2 {
            color: #555;
            margin-top: 2rem;
        }
        pre {
            background: #f5f5f5;
            padding: 1rem;
            border-radius: 4px;
            overflow-x: auto;
            border: 1px solid #ddd;
        }
        code {
            font-family: 'Monaco', 'Menlo', 'Ubuntu Mono', monospace;
        }
        .endpoint {
            background: #e8f4fd;
            padding: 1rem;
            border-radius: 4px;
            border-left: 4px solid #007acc;
            margin: 1rem 0;
        }
        .example {
            background: #f0f8f0;
            padding: 1rem;
            border-radius: 4px;
            border-left: 4px solid #28a745;
            margin: 1rem 0;
        }
        .note {
            background: #fff3cd;
            padding: 1rem;
            border-radius: 4px;
            border-left: 4px solid #ffc107;
            margin: 1rem 0;
        }
    </style>
</head>
<body>
    <h1>ImageResize - Image Resizing API</h1>
    
    <div class="endpoint">
        <h3>API Endpoint</h3>
        <code>/api/take</code>
    </div>

    <div class="example">
        <h3>Example Usage</h3>
        <code>/api/take?url=https://picsum.photos/800/600&width=400&height=300</code>
    </div>

    <div class="note">
        <h3>Note</h3>
        <p>If you only provide <code>width</code>, the height will be calculated automatically to maintain aspect ratio. Same applies if you only provide <code>height</code>.</p>
    </div>

    <h2>Available Parameters</h2>
    <pre><code>{JSON.stringify(parameters, null, 2)}</code></pre>

    <h2>Default Parameters</h2>
    <pre><code>{JSON.stringify(defaults, null, 2)}</code></pre>

    <h2>How to Use</h2>
    <p>Make a GET request to <code>/api/take</code> with your desired parameters as query strings. The service will return a resized image.</p>
    
    <p>Examples:</p>
    <ul>
        <li>Resize to specific dimensions: <code>/api/take?url=https://example.com/image.jpg&width=400&height=300</code></li>
        <li>Resize width only (maintain aspect ratio): <code>/api/take?url=https://example.com/image.jpg&width=400</code></li>
        <li>Resize height only (maintain aspect ratio): <code>/api/take?url=https://example.com/image.jpg&height=300</code></li>
        <li>Convert to PNG: <code>/api/take?url=https://example.com/image.jpg&width=400&format=png</code></li>
    </ul>
</body>
</html>
